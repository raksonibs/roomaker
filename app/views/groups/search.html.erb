Your result returned with <%= @group==nil ? "Nothing!" : @group.name %>
<% if @group!=nil %>
<% if current_user.groups.include?(@group) %>
  	<%= link_to "Leave", "groups/#{@group.id}/#{current_user.id}/leave" %><br>
  <% else %>
  	<%= link_to "Ask to Join", "/groups/#{@group.id}/ask" %><br>
<% end %>
<% end %>
Did you mean any of these?:
<% @groups.each do |group| %>
	<% if current_user.groups.include?(group) %>
  	<%= link_to "Leave #{group.name}", "groups/#{group.id}/#{current_user.id}/leave" %><br>
  <% else %>
  	<%= link_to "Ask to Join", "/groups/#{group.id}/ask" %><br>
<% end %>
<% end %>

<%= link_to "Back to Groups and Search", groups_path %>
