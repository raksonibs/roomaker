<%= form_tag "/groups/#{@group.id}/invitesearch" do %>
	Who do you want to invite to <%= @group.name %>
	 <%= label_tag :name %>
      <%= text_field_tag :name, params[:name] %>
	<%= submit_tag("Invite Friend") %>
<% end %>